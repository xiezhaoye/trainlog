# 架构

## 当前状态

本地应用已可运行：保留现有确认过的 V2 静态页面作为兼容界面，API 已迁移为 Cloudflare Worker + 本地 D1。正式邮箱认证与 React PWA 重构尚未开始。

## 目标拓扑

```text
浏览器（React PWA）
  └─ trainlog.life
       └─ Cloudflare Worker（静态资源 + /api）
            ├─ D1：用户、动作库、模板、周计划、训练记录
            ├─ KV：登录验证码、限流、短期会话辅助
            ├─ Turnstile：邮箱发送与注册防滥用
            └─ Resend：发送邮箱验证码/登录链接
```

## 技术决策

- 目标前端使用 React、TypeScript、Vite 与 PWA；移动端优先。本阶段为保证当前 UI 和操作不回退，先由 Worker 直接托管 V2 静态页面，后续以页面为单位迁入 React。
- Cloudflare Worker 同域提供静态资源和 API，避免 CORS 与双重发布。
- D1 是唯一服务端真相来源；每张业务表以 `user_id` 隔离。
- IndexedDB 保存离线草稿与待同步写入；不能把 localStorage 当作业务数据库。
- Resend 仅由 Worker 调用。API Key 只能存 Cloudflare Secret，绝不进入仓库、客户端或项目文档。
- 首期认证采用邮箱一次性验证码（OTP），而非密码；注册与登录共用同一入口。
- Google/Apple/GitHub OAuth、媒体上传、第三方日历同步不在首期范围。

## 领域模型（首期）

- `users`：账号与创建时间。
- `auth_challenges`：验证码摘要、过期时间、尝试次数；KV 为热路径，D1 仅保留必要审计信息。
- `action_library`：用户的部位与动作集合。
- `templates`：有氧或抗阻模板。
- `weekly_plan_versions`：按生效日期版本化的周计划。
- `workout_records`：训练完成记录、组数据、时长、感受和备注。

## 从 myinfo 迁移的边界

可迁移：动作库、模板、周计划、训练记录及其 JSON 数据。

不迁移：NAS Docker 部署、`node:sqlite`、myinfo 私有鉴权、`pim-service` 日历代理、内网地址与任何密钥。
