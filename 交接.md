# 交接

## 当前状态

`trainlog` 已建立独立目录、六件套文档和可运行的本地 Cloudflare Worker + D1。当前 V2 页面、图标和共享数据层来自已确认的 myinfo 训练端；Worker 已以 D1 实现模板、记录、周计划、动作库和日程查询 API。Git `main` 已推送至 `git@github.com:xiezhaoye/trainlog.git`；尚未创建线上 Cloudflare 资源。

本地认证已完成：邮箱验证码注册、密码登录、验证码登录、Cookie 会话、KV 验证码/限流均已验证；设计页保持源文件不改，Worker 在 `/login` 与 `/signup` 注入 `auth-bridge.js`。本地开发验证码只在 `AUTH_DEV_SHOW_CODE=true` 时返回，未配置或调用 Resend。

## 已确定决策

- 独立公开项目，不依赖 myinfo/NAS。
- Cloudflare 免费套餐：Worker + D1 + KV + Turnstile。
- 首期注册使用 Resend 邮箱验证码并设置密码；登录支持密码或邮箱验证码，密码最少 4 个字符。
- 数据库采用多用户 `user_id` 隔离。
- `trainlog.life` 是生产主域。
- 本地开发使用 `.dev.vars` 的 `LOCAL_DEV_TOKEN`；Worker 线上默认拒绝未认证请求，不能把该旁路部署到生产。
- 当前界面优先保持 V2 行为；后续按页面迁入 React，而不是再次整套重写。

## 接手顺序

1. 阅读 `README.md`、`架构.md`、`错题本.md`。
2. 初始化 Git 与 GitHub remote。
3. 设计稿已原样发布到本地 `/`、`/login`、`/signup`，Logo 位于 `public/logo/`；现有训练界面保留在 `/app`。后续仅接入表单逻辑，不改设计稿 HTML。
4. 在 Resend 完成发件域名验证后，将 Key 写入 Cloudflare Secret，配置 Turnstile，并做真实邮件收发与线上验证码校验。
5. 创建 Cloudflare D1/KV/Turnstile 资源，将真实 ID 写入 `wrangler.jsonc`，再进行首个 Preview 发布。
6. 继续确认公开仓库不含本地数据库、`.dev.vars`、Resend Key 和个人历史数据；后续提交直接推送 `main`。
