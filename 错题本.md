# 错题本

## 使用方式

- 警告型：记录禁止做法与原因。
- 事故型：记录日期、现象、根因、修复和教训。

## 警告

### 不要把 Resend Key 放进前端

Resend 只能由 Worker 服务端调用。任何出现在 Vite 环境变量 `VITE_*`、浏览器请求、截图或 git 历史中的 Key 都视为泄漏。

### 不要沿用 myinfo 的 SQLite/NAS 依赖

trainlog 必须可在 Cloudflare 独立部署。训练数据服务端真相来源为 D1，不能依赖 NAS 内网服务。

### 不要用 localStorage 作为训练数据真相

localStorage 只可放无敏感偏好；离线写入使用 IndexedDB，同步后以 D1 为准。

