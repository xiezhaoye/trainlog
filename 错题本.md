# 错题本

## 使用方式

- 警告型：记录禁止做法与原因。
- 事故型：记录日期、现象、根因、修复和教训。

## 警告

### 设计页不能继续使用演示认证脚本

登录与注册设计页的内联脚本仅用于演示，不能直接上线。项目通过 Worker 在公开路由注入独立 `auth-bridge.js` 接入真实 API，保留设计源文件与发布副本逐字一致。任何认证改动都不得调整页面视觉、布局或文案。

### 密码最少 4 位必须叠加服务端防护

4 位是产品明确边界，不等于安全强度。服务端必须使用强 KDF 摘要、密码登录限流、验证码尝试上限及 HttpOnly 会话 Cookie；不可只依赖浏览器的 `minlength`。

### 不要把 Resend Key 放进前端

Resend 只能由 Worker 服务端调用。任何出现在 Vite 环境变量 `VITE_*`、浏览器请求、截图或 git 历史中的 Key 都视为泄漏。

### 不要沿用 myinfo 的 SQLite/NAS 依赖

trainlog 必须可在 Cloudflare 独立部署。训练数据服务端真相来源为 D1，不能依赖 NAS 内网服务。

### 不要用 localStorage 作为训练数据真相

localStorage 只可放无敏感偏好；离线写入使用 IndexedDB，同步后以 D1 为准。
