# 部署

## 目标环境

- 域名：`trainlog.life`，已在 Cloudflare 托管。
- 运行平台：Cloudflare 免费套餐。
- 代码仓库：`git@github.com:xiezhaoye/trainlog.git`。

## 本地开发

```bash
npm install
cp .dev.vars.example .dev.vars
npm run db:migrate
npm run db:seed
npm run dev
```

访问 `http://localhost:8788`。本地 D1 数据保存在 `.wrangler/`；删掉该目录可获得干净数据。`.dev.vars` 的开发令牌仅用于本机，不能部署到 Cloudflare。

## 计划中的 Cloudflare 资源

1. 一个 Worker/Pages 项目，绑定 `trainlog.life` 与 `www.trainlog.life`（后者重定向到主域）。
2. 一个 D1 数据库。
3. 一个 KV namespace，用于 OTP、会话辅助与限流。
4. 一个 Turnstile widget，用于认证发送接口。
5. Worker Secret：`RESEND_API_KEY`、`AUTH_SECRET`、必要的 Turnstile Secret。

所有 Secret 仅通过 `wrangler secret put` 或 Cloudflare Dashboard 写入；不写入 `.dev.vars`、提交记录或文档正文。

本地默认不调用 Resend；`AUTH_DEV_SHOW_CODE=true` 时仅在本地 API 响应中返回开发验证码。生产配置 `RESEND_API_KEY` 前，必须先在 Resend 验证发件域名并完成 SPF/DKIM 配置。

## 发布策略

- GitHub `main` 推送触发 Cloudflare 生产发布。
- Pull Request 使用 Preview Deployment。
- D1 migration 先在 preview/staging 验证，再对生产执行。
- 公开发布前必须验证：登录邮件、跨用户数据隔离、训练记录 CRUD、PWA 安装与移动端窄屏。

## 回滚

- 前端/Worker：在 Cloudflare Dashboard 回滚到上一个部署。
- 数据库：migration 只追加；每次 schema 变更必须提供显式的前向修复或回滚 migration，禁止手工修改生产表结构。

## 国内下载约定

依赖安装优先使用 npm 国内镜像 `https://registry.npmmirror.com`；部署前如需下载其他依赖，同样优先使用可靠国内镜像。
