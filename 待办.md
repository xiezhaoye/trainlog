# 待办

## 初始化

- [x] 初始化 Git 仓库并关联 `git@github.com:xiezhaoye/trainlog.git`（`main` 已推送）。
- [x] 建立 Cloudflare Worker + 本地 D1 工程，并可在 `localhost:8788` 运行当前 V2 页面。
- [ ] 配置 Turnstile、生产 Secrets，并将 `trainlog.life` 的旧 DNS 源站替换为 Worker Custom Domain。
- [x] 建立邮箱验证码注册、密码登录 / 验证码登录、会话与限流（密码最少 4 个字符）。
- [ ] 在 Resend 验证发信域名后，配置 Cloudflare Secret 并做真实邮件收发验证。

## 训练领域

- [x] 定义 D1 schema 与本地 migration；V2 演示数据仅由 `npm run db:seed` 写入本机。
- [x] 实现动作库、模板、周计划、训练记录和日程查询 API。
- [ ] 实现离线草稿与同步队列。
- [ ] 从 myinfo SQLite 导出并导入个人历史数据。

## 产品与发布

- [ ] 将确认后的 V2 UI 重构为 React 组件与设计 token。
- [ ] 建立单元、API 与关键移动端流程测试。
- [ ] 绑定 `trainlog.life` / `www.trainlog.life`；Worker 已发布至 `trainlog.xiezhaoye.workers.dev`。
- [ ] 添加隐私说明、数据导出与账号删除流程。
