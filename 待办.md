# 待办

## 初始化

- [x] 初始化 Git 仓库并关联 `git@github.com:xiezhaoye/trainlog.git`（`main` 已推送）。
- [x] 建立 Cloudflare Worker + 本地 D1 工程，并可在 `localhost:8788` 运行当前 V2 页面。
- [ ] 配置 Cloudflare 生产 D1、KV、Turnstile 和 Preview/Production 环境。
- [ ] 建立 Resend OTP 邮箱认证流程与限流。

## 训练领域

- [x] 定义 D1 schema 与本地 migration（含开发动作库种子）。
- [x] 实现动作库、模板、周计划、训练记录和日程查询 API。
- [ ] 实现离线草稿与同步队列。
- [ ] 从 myinfo SQLite 导出并导入个人历史数据。

## 产品与发布

- [ ] 将确认后的 V2 UI 重构为 React 组件与设计 token。
- [ ] 建立单元、API 与关键移动端流程测试。
- [ ] 绑定 `trainlog.life` / `www.trainlog.life`，完成 Cloudflare 发布。
- [ ] 添加隐私说明、数据导出与账号删除流程。
